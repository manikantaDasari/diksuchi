fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pyyaml>=6.0.1
tiktoken>=0.7.0
python-dotenv>=1.0.1
pydantic>=2.7.0
aiofiles>=23.2.1          # Required by FastAPI's FileResponse (dashboard serving)
# Centroid classifier — 22MB model, CPU-only, ~8ms inference
# Install once: pip install sentence-transformers
# Router runs without it (falls back to keyword matching)
sentence-transformers>=2.7.0
